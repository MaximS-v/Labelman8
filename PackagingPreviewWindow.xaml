<local:StorableWindow x:Class="Labelman8.PackagingPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:local="clr-namespace:Labelman8"
        Title="Упаковка" 
        Width="1000" Height="800"
        WindowStartupLocation="CenterOwner"
        MinHeight="600" MinWidth="800">

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10" HorizontalAlignment="Center">
			<Button x:Name="btnPrev" Content="◀ Предыдущая" Width="120" Height="30" Margin="5" Click="BtnPrev_Click"/>
			<TextBlock x:Name="txtPageInfo" Text="Страница 1 из 1" VerticalAlignment="Center" Margin="15,0" FontSize="14"/>
			<Button x:Name="btnNext" Content="Следующая ▶" Width="120" Height="30" Margin="5" Click="BtnNext_Click"/>
			<Button x:Name="btnPrint" Content="🖨️ Печать" Width="120" Height="30" Margin="15,5,5,5" Click="BtnPrint_Click"/>
			<Button x:Name="btnClose" Content="✖ Закрыть" Width="120" Height="30" Margin="5" Click="BtnClose_Click"/>
		</StackPanel>

		<ScrollViewer Grid.Row="1" 
                      HorizontalScrollBarVisibility="Auto" 
                      VerticalScrollBarVisibility="Auto"
                      Background="LightGray"
                      Margin="20">
			<Border BorderBrush="DarkGray" BorderThickness="1" Background="White"
                    HorizontalAlignment="Center" VerticalAlignment="Center">
				<Image x:Name="pageImage" Stretch="None"/>
			</Border>
		</ScrollViewer>

		<StatusBar Grid.Row="2">
			<StatusBarItem>
				<TextBlock x:Name="txtStatus" Text="Всего элементов: 0" />
			</StatusBarItem>
		</StatusBar>
	</Grid>
</local:StorableWindow>